- cli-аргументы и средства ввода под новый функционал
  ```yaml:attrs
  assignee: Andrey Shigantsov
  ```

  Хорошо бы подумать и разработать cli-аргументы и средства ввода для тулзы под новый функционал. Например, как указывать фильтры и параметры поиска? Как редактировать задачи или отмечать выполненные? И т.д. В целом хорошо бы поразмыслить, какие ещё команды нужны и как должен выглядеть для них интерфейс и как они могут быть вписаны в существующую концепцию.

- Перейти на `markdown-rs` в `tracker-fs`
    ```yaml:attrs
    assignee: Andrey Shigantsov
    tags: [refactor]
    ```

- Реализовать работу с задачами в отдельных файлах, организовынных в субдиректориях относительно корня ISSUES
    *Andrey Shigantsov*: могу взять в работу потом

- Учитывать задачи с установленным [x], но не [==> ] (задавать в конфигурации допустимые символы) как выполненные
- Реализовать редактирование текста задачи, добавление отметки о выполнении
- Добавить режим вывода задач до первого milestone
- Переделать конфиг Display для отображения режимов `compact` и `pretty` по образцу `WorkingMode`
- Колоризовать вывод в терминал, добавить настройки цветовых схем
- Добавить фильтр по тегам, команда `--tags` (`--step-tags`, `--project-tags`) со списком включаемых и исключаемых тегов
- Поддержать обратный порядок списка задач (для календаря)
- Добавить команду `calendar` и/или режим проекта "календарь" (определённая организация вех и задач с автоматической перестановкой и логированием)
- Добавить документацию проекта
- Переместить `FsProjectMetadata` в `todo_tracker_fs`
- Создать `FsProjectsSearcher` для удобного поиска проектов вместо используемого в `open_tracker` подхода
- Посмотреть другие утилиты управления задачами и выбрать, что у них перенять
- Определить, какие данные и в каком виде должна выводить команда `info`
- Сделать настраиваемый шаблон для отображения содержимого задачи в файле и в выводе команд

# Выполненные

- [x] Добавить перезаписываемые настройки каждой команде - в общем конфиге и непосредственно при вызове
    ```yaml:attrs
    assignee: Andrey Shigantsov
    ```

- [x] Добавить поддержку загрузки контента задачи из файла
    ```yaml:attrs
    assignee: Andrey Shigantsov
    tags: [feature]
    ```
    Предлагается, для начала, добавить поддержку простой ссылки на файл в заголовке:
    ```md
    - Добавь что-нибудь в проект
       Здесь будет описание..
    - [Добавь что-то ещё и еще. Это просто заголовок-ссылка на детальное описание. Должен быть коротким, по идее. Но и длинные не проблема](<relative-file-path>) [//]:#(остальное сохраняется в `trailing-content`, который можно будет либо игнорировать, либо мержить с контентом из файла)
    - ...
    ```
    >NOTE: `[//]:#(...)` - это способ создавать комментарии в markdown файлах

- [x] Не учитывать регистр при указании `WorkingMode` в конфиге
